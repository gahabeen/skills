interface:
  display_name: "510"
  short_description: "Explain, explore, build, debug, review, and prepare PRs"
  default_prompt: "$510 Show me a short guide to what I can do next."
policy:
  allow_implicit_invocation: false
